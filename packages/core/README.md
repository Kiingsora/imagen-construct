# Core package

Planned model-independent TypeScript package containing:

- project and layer types;
- scene commands;
- serialization and migrations;
- validation;
- undoable editor operations;
- adapter capability types shared with the editor.

The core package must not depend on React, Konva, ComfyUI, or a specific image model.
